import colors from './colors';
import { StyleSheet } from 'react-native';

export const helpers = StyleSheet.create({
  shadow: {
      shadowColor: '#000',
      shadowOffset: {
          width: 0,
          height: 5,
      },
      shadowOpacity: 0.36,
      shadowRadius: 6.68,

      elevation: 6,
  },
});
